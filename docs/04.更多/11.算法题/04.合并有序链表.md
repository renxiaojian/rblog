---
title: 合并有序链表
date: 2021-07-12 16:54:22
permalink: /pages/aad5f3/
categories:
  - 更多
  - 算法题
tags:
  - 
---

## 1 题目

将两个有序的链表合并为一个新链表，要求新的链表是通过拼接两个链表的节点来生成的，且合并后新链表依然有序。

**示例**

```
输入：{2},{1}
返回值：{1,2}
```

## 2 题解

```
/*
 * function ListNode(x){
 *   this.val = x;
 *   this.next = null;
 * }
 */

/**
  * 
  * @param l1 ListNode类 
  * @param l2 ListNode类 
  * @return ListNode类
  */
function mergeTwoLists( l1 ,  l2 ) {
    const pHead = {
        next: null
    }
    let pre = pHead
    while(l1 || l2) {
        if(!l2 || (l1 && l1.val < l2.val)) {
            let next = l1.next
            pre.next = l1
            l1 = next
        } else {
            let next = l2.next
            pre.next = l2
            l2 = next
        }
        pre = pre.next
    }
    return pHead.next
}
module.exports = {
    mergeTwoLists : mergeTwoLists
};
```

